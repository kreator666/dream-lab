# 任务执行记录

## 任务信息
- **阶段**: 第 1 阶段 —— 项目初始化与 MVP 骨架
- **任务编号**: 1.2
- **任务名称**: 实现规则汇总页与详情页，连接 PostgreSQL
- **执行日期**: 2026-09-02

## 任务说明
在任务 1.1 搭建的工程基础上，连接 PostgreSQL 数据库，实现 Prop Firm 规则汇总列表页和详情页，完成数据库迁移与种子数据。

## 完成内容
- 新增 `FirmCard` 组件，展示平台 Logo 占位、难度星、折扣码、简介
- 实现 `/firms` 列表页，使用 Prisma `findMany` 从 PostgreSQL 动态读取并排序
- 实现 `/firms/[slug]` 详情页，展示平台简介、价格、规则要点、折扣码复制、官网外链
- 新增 `CopyButton` 客户端组件，用于复制折扣码
- 启动 PostgreSQL 17 Docker 容器，发现本地 5432 端口被 `mrc-db` 占用，改映射到 5433
- 修复 Prisma 连接字符串：Windows 下 `localhost:5433` 解析到 IPv6，改用 `127.0.0.1:5433`
- 运行 `prisma migrate dev --name init` 与 `npm run db:seed`
- 种子数据：1 个 admin 用户 + 3 个平台（Lucid / TPT / FundedNext）+ 2 篇教程占位

## Commit 记录
- **Commit ID**: `9aa6134c64cb9b8c1b79dca66a58e15c0f0f30e0`
- **Commit Message**: `task 1.2: 实现规则汇总页与详情页，连接 PostgreSQL`
- **Branch**: `main`
- **Remote**: `origin/main`

## 备注
- 测试通过率：`npm run build` 通过；`/firms` 和 `/firms/[slug]` 被标记为动态路由
- 环境注意：本地开发需运行 `cd web && docker compose up -d`，并确保 `.env` 中 `DATABASE_URL` 使用 `127.0.0.1:5433`
- `.env` 已加入 `.gitignore`，不提交仓库
- 下一步：任务 1.3 实现教程中心（/articles）+ 后台内容管理雏形
