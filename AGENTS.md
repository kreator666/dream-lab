# Comedy Agent —— Agent 协作规范

## 一、任务执行模式

### 1.1 默认模式（单任务执行）

**触发条件**：用户未明确下达 `"连续执行"` 指令时，默认采用此模式。

**执行流程**：
1. **开发** —— 完成当前任务的功能实现
2. **测试** —— 编写并运行测试用例，确保通过
3. **提交** —— `git add -A && git commit -m "task X.Y: <任务描述>"`
4. **推送** —— `git push origin feature`
5. **记录** —— 在 `vibe-log/` 目录下创建任务日志文件
6. **等待** —— **停止执行，等待用户的下一步指示，不得擅自开始下一个任务**

### 1.2 连续执行模式

**触发条件**：用户明确下达包含 `"连续执行"` 的指令时，采用此模式。

**执行流程**：
1. **开发** —— 完成功能实现
2. **测试** —— 编写并运行测试用例
3. **提交 & 推送** —— `git commit` + `git push origin feature`
4. **记录** —— 写入 `vibe-log/`
5. **继续** —— 自动推进到同一阶段的下一个任务，循环执行上述流程，直到该阶段所有任务完成或用户叫停

## 二、Git 提交规范

### Commit Message 格式

```
task X.Y: <任务名称>

- 变更点 1
- 变更点 2
- ...
```

**示例**：
```
task 3.1: 文档加载流水线

- 新增 DocumentLoader 支持 PDF/Word/网页/文本/字幕解析
- 内置 SRT/VTT/ASS 字幕解析器，保留时间码元数据
- 新增 12 个单元测试全部通过
```

## 三、vibe-log 记录规范

### 日志文件命名

```
vibe-log/YYYY-MM-DD-task-X.Y.md
```

若同一天有多个任务，按时间顺序递增编号。

### 日志内容模板

```markdown
# 任务执行记录

## 任务信息
- **阶段**: 第 X 阶段 —— <阶段主题>
- **任务编号**: X.Y
- **任务名称**: <任务名称>
- **执行日期**: YYYY-MM-DD

## 任务说明
<简述任务目标和说明>

## 完成内容
- <变更点 1>
- <变更点 2>
- ...

## Commit 记录
- **Commit ID**: `<完整 commit hash>`
- **Commit Message**: `<完整 message>`
- **Branch**: `feature`
- **Remote**: `origin/feature`

## 备注
- 测试通过率: X/Y (Z%)
- 其他需要说明的事项
```

## 四、关键原则

1. **未经指令不推进**：默认模式下，完成一个任务后必须停止，等待用户明确指示后再开始下一个任务。
2. **测试必通过**：每个任务提交前，必须确保相关测试通过。全量测试失败时，优先修复再提交。
3. **最小变更**：每个 commit 只包含当前任务的变更，避免混入了其他任务的代码。
4. **及时记录**：commit 后立即写入 vibe-log，确保执行记忆完整可追溯。
